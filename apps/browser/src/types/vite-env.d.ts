/// <reference types="vite/client" />

/**
 * The app's own version, substituted at build time by Vite's `define` from
 * apps/browser/package.json. Rendered in the Settings panel's About block.
 */
declare const __APP_VERSION__: string;
