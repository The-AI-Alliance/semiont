/**
 * Annotation Assembly — re-exported from @semiont/core
 *
 * The canonical implementation lives in @semiont/core.
 * This re-export maintains the public API of @semiont/make-meaning.
 */

export {
  assembleAnnotation,
  applyBodyOperations,
  type AssembledAnnotation,
} from '@semiont/core';
