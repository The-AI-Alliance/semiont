{
  "name": "@semiont/config",
  "version": "0.1.0",
  "description": "Shared configuration loading for Semiont",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "keywords": ["semiont", "config", "configuration"],
  "author": "Semiont Team",
  "license": "MIT",
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
