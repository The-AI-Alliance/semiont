{
  "name": "@semiont/config",
  "version": "0.1.0",
  "description": "Semiont configuration loader - reads JSON config files from /config/environments/",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "typescript": "^5.0.0"
  }
}
