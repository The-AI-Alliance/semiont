{
  "post": {
    "summary": "Bind Annotation Body (SSE Stream)",
    "description": "Apply annotation body operations and stream completion via Server-Sent Events",
    "tags": [
      "Resources",
      "Annotations",
      "Real-time"
    ],
    "security": [
      {
        "bearerAuth": []
      }
    ],
    "parameters": [
      {
        "schema": {
          "type": "string"
        },
        "required": true,
        "name": "resourceId",
        "in": "path",
        "description": "Resource ID"
      },
      {
        "schema": {
          "type": "string"
        },
        "required": true,
        "name": "annotationId",
        "in": "path",
        "description": "Annotation ID"
      }
    ],
    "requestBody": {
      "content": {
        "application/json": {
          "schema": {
            "$ref": "../components/schemas/BindAnnotationStreamRequest.json"
          }
        }
      }
    },
    "responses": {
      "200": {
        "description": "SSE stream opened successfully",
        "content": {
          "text/event-stream": {
            "schema": {
              "$ref": "../components/schemas/EventStreamResponse.json"
            }
          }
        }
      },
      "401": {
        "description": "Authentication required"
      },
      "404": {
        "description": "Annotation not found"
      }
    }
  }
}
