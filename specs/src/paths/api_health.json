{
  "get": {
    "summary": "Health Check",
    "description": "Check if the API is operational. Served at both /api/health and /, so a person who types the host gets an answer rather than a 404.",
    "tags": [
      "System"
    ],
    "responses": {
      "200": {
        "description": "Health status of the API",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "../components/schemas/HealthResponse.json"
            }
          }
        }
      }
    }
  }
}