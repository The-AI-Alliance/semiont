{
  "type": "object",
  "properties": {
    "issuer": {
      "type": "string",
      "nullable": true,
      "description": "The OIDC issuer this knowledge base trusts for human tokens, exactly as it appears in a token's iss claim. Null when no identity provider is configured, in which case nobody can sign in."
    },
    "audience": {
      "type": "string",
      "nullable": true,
      "description": "The value the gateway requires in a token's aud claim. Null when no identity provider is configured."
    }
  },
  "required": [
    "issuer",
    "audience"
  ]
}
