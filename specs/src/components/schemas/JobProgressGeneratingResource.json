{
  "type": "object",
  "description": "The model is generating the resource.",
  "properties": {
    "code": {
      "type": "string",
      "enum": [
        "generating-resource"
      ]
    }
  },
  "required": [
    "code"
  ]
}
