{
  "type": "object",
  "description": "Writing the generated resource.",
  "properties": {
    "code": {
      "type": "string",
      "enum": [
        "creating-resource"
      ]
    }
  },
  "required": [
    "code"
  ]
}
