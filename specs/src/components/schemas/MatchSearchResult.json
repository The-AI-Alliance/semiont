{
  "type": "object",
  "description": "Search results payload emitted on match:search-results SSE channel.",
  "properties": {
    "correlationId": {
      "type": "string"
    },
    "referenceId": {
      "type": "string"
    },
    "response": {
      "type": "array",
      "description": "The scored candidates, best first. This IS the result list — there is no wrapper object with a `candidates` or `resources` key around it.",
      "items": { "$ref": "ScoredResource.json" }
    }
  },
  "required": ["correlationId", "referenceId", "response"]
}
