{
  "type": "object",
  "description": "Search results payload emitted on match:search-results SSE channel.",
  "properties": {
    "referenceId": {
      "type": "string"
    },
    "response": {
      "type": "array",
      "description": "The scored candidates, best first.",
      "items": {
        "$ref": "ScoredResource.json"
      }
    }
  },
  "required": [
    "referenceId",
    "response"
  ]
}
