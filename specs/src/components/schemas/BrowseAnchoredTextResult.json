{
  "type": "object",
  "description": "A resource's coordinate map, or null when none has been derived. Null is the common case and not an error: a native text layer is read in the browser, and a media type with no extractor never produces one.",
  "properties": {
    "correlationId": {
      "type": "string"
    },
    "response": {
      "oneOf": [
        { "$ref": "./AnchoredText.json" },
        { "type": "null" }
      ]
    }
  },
  "required": ["correlationId", "response"]
}
