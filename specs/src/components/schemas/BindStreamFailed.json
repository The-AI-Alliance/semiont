{
  "type": "object",
  "description": "Error payload emitted on bind:failed SSE channel.",
  "properties": {
    "correlationId": {
      "type": "string",
      "description": "Echoes the correlationId from the originating bind:update-body command, if any."
    },
    "error": {
      "type": "string"
    }
  },
  "required": ["error"]
}
