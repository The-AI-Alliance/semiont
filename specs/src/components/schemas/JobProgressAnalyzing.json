{
  "type": "object",
  "description": "Analyzing the content.",
  "properties": {
    "code": {
      "type": "string",
      "enum": [
        "analyzing"
      ]
    }
  },
  "required": [
    "code"
  ]
}
