{
  "type": "object",
  "description": "Analyzing the content against the tag schema.",
  "properties": {
    "code": {
      "type": "string",
      "enum": [
        "analyzing-tags"
      ]
    }
  },
  "required": [
    "code"
  ]
}
