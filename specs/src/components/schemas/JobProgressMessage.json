{
  "description": "What a running job is doing right now, as a code plus typed params — never a prose sentence. The producer reports what happened; each client renders it in the user's language (react-ui from its translations, the Go launcher from its English map). The vocabulary is the census of every onProgress call site in @semiont/jobs (.plans/ASSIST-PROGRESS-CONSOLIDATION.md P1, 2026-08-12); extending it means adding a variant here and copy in every client, gated by the locale-completeness check.",
  "oneOf": [
    {
      "type": "object",
      "description": "Codes that carry no params. `complete-generated` is generation's terminal success and is deliberately generic: the client already holds the title it typed, and the outcome (name + resource link) travels on job:complete, not on progress (GENERATE-FROM-RESOURCE D7/D8).",
      "properties": {
        "code": {
          "type": "string",
          "enum": ["loading", "analyzing", "analyzing-tags", "generating-resource", "creating-resource", "complete-generated"]
        }
      },
      "required": ["code"]
    },
    {
      "type": "object",
      "description": "Entity detection, one entity type at a time.",
      "properties": {
        "code": { "type": "string", "enum": ["detecting-entities"] },
        "entityType": { "type": "string", "description": "Entity type currently being detected" }
      },
      "required": ["code", "entityType"]
    },
    {
      "type": "object",
      "description": "Writing detected annotations back to the resource.",
      "properties": {
        "code": { "type": "string", "enum": ["creating-annotations", "creating-tag-annotations"] },
        "count": { "type": "integer", "description": "How many annotations are being created" }
      },
      "required": ["code", "count"]
    },
    {
      "type": "object",
      "description": "Terminal success summary.",
      "properties": {
        "code": { "type": "string", "enum": ["complete-created"] },
        "count": { "type": "integer", "description": "How many annotations were created" },
        "kind": {
          "type": "string",
          "enum": ["highlight", "comment", "assessment", "reference", "tag"],
          "description": "What kind of annotation was created; clients pluralize/translate"
        }
      },
      "required": ["code", "count", "kind"]
    }
  ]
}
