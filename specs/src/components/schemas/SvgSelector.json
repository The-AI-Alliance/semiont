{
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "SvgSelector"
      ]
    },
    "value": {
      "type": "string",
      "description": "SVG markup defining the region (must include xmlns attribute)"
    }
  },
  "required": [
    "type",
    "value"
  ]
}
