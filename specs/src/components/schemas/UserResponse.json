{
  "type": "object",
  "description": "The authenticated principal, as this knowledge base names it.\n\nThe `did` is the identity: it is what the bus stamps on every event, what resource creation is attributed to, and what a client must compare against to recognise its own work in the data. The row id this endpoint used to return appears nowhere else in the system, so it could not be correlated with anything and is gone.\n\nThe remaining fields exist to be displayed. Roles are carried but gate nothing here.",
  "properties": {
    "did": {
      "type": "string",
      "description": "The authenticated principal's DID — `did:web:<domain>:users:<email>` for a person, `did:web:<domain>:agents:<provider>:<model>` for a software agent."
    },
    "email": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "nullable": true
    },
    "image": {
      "type": "string",
      "nullable": true
    },
    "domain": {
      "type": "string",
      "description": "Not always the email's suffix: a software agent's email sits in an `agents.<host>` namespace while its domain is the deployment's."
    },
    "isAdmin": {
      "type": "boolean",
      "description": "Carried, read by nothing. No gateway route grants access on this basis."
    },
    "isModerator": {
      "type": "boolean",
      "description": "Carried, read by nothing. No gateway route grants access on this basis."
    }
  },
  "required": [
    "did",
    "email",
    "name",
    "image",
    "domain",
    "isAdmin",
    "isModerator"
  ]
}
